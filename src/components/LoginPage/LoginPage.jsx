import React from 'react';
import PropTypes from 'prop-types';

const LoginPage = ({ updateDashboard }) => {

  return(
    <div>

    </div>
  )
};

LoginPage.propTypes = {
  updateDashboard: PropTypes.func,
};

export default LoginPage;
